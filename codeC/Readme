WildWater – Projet MI1E
📌 Présentation

WildWater est un projet en C visant à analyser un réseau de distribution d’eau à partir de fichiers CSV volumineux.
Le programme permet de traiter différents modes d’analyse (historique, sources, fuites) en s’appuyant sur des structures de données efficaces (AVL + arbre classique).

⚙️ Fonctionnalités principales
🔹 histo

    Analyse des capacités et volumes (max, src, real)
    Utilise un AVL indexé par identifiant
    Sortie optimisée pour grands fichiers

🔹 src

    Calcul des volumes issus des sources
    Agrégation rapide via AVL

🔹 real

    Traitement des volumes réellement distribués
    Génère un fichier intermédiaire returnreal.txt

🔹 leaks (avec bonus)

    Construction complète du réseau hydraulique de l’usine
    Détection du tronçon qui perd le plus d’eau
    Navigation interactive dans le réseau via un mode debug

Architecture :

    AVL = index rapide (ID → données)
    Arbre classique (enfant / frère) = topologie du réseau

🧠 Architecture des données

    AVL
    Indexation par identifiant
    Union pour gérer :

        NodeH (historique)
        NodeL (fuites)

    Arbre classique (AC)

        Représentation du réseau
        Chaque nœud pointe vers ses données via NodeL

            Cette séparation permet :
                des recherches rapides (O(log n))
                une topologie simple et lisible
                aucune duplication inutile des données

🧪 Mode Debug (leaks)

Commandes disponibles :

p        afficher le noeud courant
ls       lister les enfants
cd <i>   se déplacer vers un enfant
up       remonter
root     revenir à la racine
path     afficher le chemin courant
q        quitter


🛠️ Compilation
✅ Prérequis

    Make
    GCC (Linux / MinGW sous Windows)
    Valgrind pour verifier les pertes de données


▶️ Compiler
    make

🧹 Nettoyer
    make clean
    make cleanexec (pour clear les ficher .o)


Le Makefile est pour Linux (fonctionne très bien sur mon VPS avec Ubuntu)

▶️ Exécution

Exemple :

./script.sh data.csv leaks/histo "Nom d'usine"/src/real/max/all


Les scripts .sh fournis facilitent l’exécution selon le mode :

    Bonusall.sh
    src.sh
    real.sh
    leaks.sh
    max.sh

📁 Organisation du projet
.
├── codeC/
│   ├── AVL.c / .h
│   ├── leaks.c / .h
│   ├── histo.c / .h
│   ├── ArbreC.c / .h
│   ├── code.c
│   ├── head.h
│   ├── type.h
│   └── build/
│       └── *.o
├── Makefile
├── *.sh
├── returnleaks.dat
└── README.md